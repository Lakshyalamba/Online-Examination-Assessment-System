# Commit And Push Guide

Use this guide across all Ravleen prompts.

- one numbered prompt should usually produce one commit
- keep grading logic, queue UI, and publish controls in separate commits
- do not mix scoring-rule changes with broad UI cleanup
- push after each stable step commit so the state model stays reviewable
